'use strict';

const expect = require('chai').expect;

describe('Catalog routes', function() {

	it('Should return all categories');

	it('Should return a category details');

	it('Should return featured product list');

	it('Should return a category products');

	it('Should return a product details');

});
